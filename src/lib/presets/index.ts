/**
 * Presets Module
 *
 * Save, load, and share filter chain presets.
 */

export { presets, PresetManager } from './preset-manager';
export type { ImportResult, CreatePresetOptions } from './preset-manager';
